export type DateString = string
export type EssayToken = string
export type ProjectDate = {
  year: number,
  week: number
}
export type WeekFormat = {
  week_id: {
    study_year_from: number,
    study_year_to: number,
    week_number: number
  }
}